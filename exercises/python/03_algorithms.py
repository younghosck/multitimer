"""
클로드 스킬 실습 - Python 알고리즘

정렬, 검색, 재귀 등 기본 알고리즘 실습입니다.
"""


def binary_search(sorted_list: list[int], target: int) -> int:
    """정렬된 리스트에서 이진 탐색을 수행합니다.

    Args:
        sorted_list: 정렬된 정수 리스트
        target: 찾을 값

    Returns:
        찾은 인덱스, 없으면 -1
    """
    # TODO: 구현하세요
    pass


def bubble_sort(items: list[int]) -> list[int]:
    """버블 정렬을 구현합니다.

    Args:
        items: 정렬할 리스트

    Returns:
        정렬된 새 리스트
    """
    # TODO: 구현하세요
    pass


def fibonacci(n: int) -> int:
    """n번째 피보나치 수를 반환합니다.

    Args:
        n: 인덱스 (0부터 시작)

    Returns:
        n번째 피보나치 수

    Example:
        >>> [fibonacci(i) for i in range(7)]
        [0, 1, 1, 2, 3, 5, 8]
    """
    # TODO: 구현하세요
    pass


def factorial(n: int) -> int:
    """n의 팩토리얼을 계산합니다.

    Args:
        n: 양의 정수

    Returns:
        n!
    """
    # TODO: 구현하세요
    pass


def is_prime(n: int) -> bool:
    """숫자가 소수인지 확인합니다.

    Args:
        n: 확인할 숫자

    Returns:
        소수면 True
    """
    # TODO: 구현하세요
    pass


def gcd(a: int, b: int) -> int:
    """두 숫자의 최대공약수를 구합니다.

    Args:
        a: 첫 번째 숫자
        b: 두 번째 숫자

    Returns:
        최대공약수
    """
    # TODO: 구현하세요
    pass
